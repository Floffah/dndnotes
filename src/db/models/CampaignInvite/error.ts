export enum CampaignInviteError {
    ALREADY_MEMBER = "User is already a member of this campaign",
    ALREADY_INVITED = "User has already been invited to this campaign",
    INVALID_CODE = "Invalid campaign invite code",
    NOT_FOUND = "Campaign invite not found",
}
